# Copyright Contributors to the OpenVDB Project
# SPDX-License-Identifier: Apache-2.0

name: Sync Doc Version

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-version:
    if: github.repository == 'openvdb/fvdb-core'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          # `release: published` checks out the tag (detached HEAD); pin to the
          # default branch so the doc-version PR is always based on main.
          ref: main
          persist-credentials: false

      - name: Get latest release version
        id: release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="$(gh release view --json tagName -q .tagName 2>/dev/null || echo "")"
          if [[ -z "$TAG" ]]; then
            echo "No GitHub releases found; nothing to sync."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          VERSION="${TAG#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Latest release: $TAG (version $VERSION)"

      - name: Extract versions.json from the release tag
        if: steps.release.outputs.skip != 'true'
        env:
          TAG: ${{ steps.release.outputs.tag }}
        run: |
          # Read the released torch/cuda/python versions from the tag itself
          # (not from main, which may already target the next release).
          git fetch --depth 1 origin "refs/tags/${TAG}:refs/tags/${TAG}"
          git show "${TAG}:.github/versions.json" > "${RUNNER_TEMP}/release-versions.json"

      - name: Get current doc version
        if: steps.release.outputs.skip != 'true'
        id: current
        run: |
          CURRENT="$(grep '^fvdb_core_stable_version = ' docs/conf.py \
            | sed 's/^fvdb_core_stable_version = "\(.*\)"/\1/')"
          echo "version=$CURRENT" >> "$GITHUB_OUTPUT"
          echo "Current doc version: $CURRENT"

      - name: Update doc version
        if: >-
          steps.release.outputs.skip != 'true' &&
          steps.release.outputs.version != steps.current.outputs.version
        run: |
          echo "Updating doc version from ${STEPS_CURRENT_OUTPUTS_VERSION} to ${STEPS_RELEASE_OUTPUTS_VERSION}"
          ./devtools/update-doc-versions.sh "${STEPS_RELEASE_OUTPUTS_VERSION}" \
            --versions-json "${RUNNER_TEMP}/release-versions.json"
        env:
          STEPS_CURRENT_OUTPUTS_VERSION: ${{ steps.current.outputs.version }}
          STEPS_RELEASE_OUTPUTS_VERSION: ${{ steps.release.outputs.version }}

      - name: Create pull request
        if: >-
          steps.release.outputs.skip != 'true' &&
          steps.release.outputs.version != steps.current.outputs.version
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          commit-message: "Update doc stable version to ${{ steps.release.outputs.version }}"
          signoff: true
          branch: auto/sync-doc-version
          title: "Update doc stable version to ${{ steps.release.outputs.version }}"
          body: |
            Automated PR to update the stable release metadata in `docs/conf.py`
            (`fvdb_core_stable_version` plus the released PyTorch/CUDA/Python
            versions) to match the latest GitHub Release
            (`v${{ steps.release.outputs.version }}`), and to remove the
            pre-release marker for this minor version from
            `docs/installation.rst` if present.

            If the set of supported CUDA versions changed in this release,
            check that `docs/installation.rst` has a wheel-install example for
            each entry in `fvdb_core_stable_cuda_versions`.

            When merged, a docs rebuild will be triggered automatically.
          labels: automation
